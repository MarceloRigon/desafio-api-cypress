{
  "name": "desafio-api-cypress",
  "version": "1.0.0",
  "description": "Projeto de testes automatizados de API com Cypress",
  "scripts": {
  "test": "npx cypress run --reporter mochawesome --reporter-options reportDir=cypress/reports,overwrite=false,html=false,json=true",
    "test:full": "npm run test"
},
  "devDependencies": {
    "ajv": "^8.12.0",
    "cypress": "^13.15.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0"
  }
}
